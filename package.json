{
  "name": "investment-ai",
  "version": "1.0.0",
  "description": "Investment AI application with client and server",
  "main": "index.js",
  "scripts": {
    "server": "cd Server && yarn start",
    "client": "cd Client && yarn dev",
    "dev": "concurrently \"npm run server\" \"wait-on http://localhost:3001/api/health && npm run client\"",
    "start": "npm run dev"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "nodemon": "^3.1.2",
    "wait-on": "^7.2.0"
  }
}
